import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        borderWq: 0.9,
        borderRadius: 0.5,
        borderColor: "#ddd",
        padding: 15,
        marginBottom: 15
    },
    title: {
        fontWeight: "bold",
        paddingBottom: 5
    }
});