import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    titleItem: {
        fontWeight: "bold",
    },
    titleLogoutItem: {
        fontWeight: "bold",
        color: "#BB0000"
    },
});