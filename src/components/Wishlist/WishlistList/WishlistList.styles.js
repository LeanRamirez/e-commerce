import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        padding: 10
    },
    title: {
        fontWeight: "bold",
        fontSize: 19,
        marginBottom: 5
    }
});
