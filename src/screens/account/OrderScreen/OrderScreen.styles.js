import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        padding: 10
    },
    title: {
        fontWeight: "bold",
        fontSize: 22,
    },
    totalPayment: {
        textAlign: "center",
        fontWeight: "bold",
        fontSize: 22,
        marginBottom: 30
    }
});