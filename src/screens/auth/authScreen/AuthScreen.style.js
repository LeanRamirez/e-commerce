import { LogBox, StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        padding: 20
    },
    logo: {
        width: "100%",
        height: 300,
        resizeMode: "contain",
        marginBottom: -15
    }
});