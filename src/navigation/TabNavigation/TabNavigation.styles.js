import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    icon: {
        fontSize: 25,
        backgroundColor: "#16222b"
    },
    tabBar: {
        backgroundColor: "#16222b",
    },
    totalCart: {
        position: "absolute",
        top: -5,
        right: -10
    },
});